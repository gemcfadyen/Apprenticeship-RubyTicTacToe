require 'spec_helper'

describe Ttt::Core do
  it 'has a version number' do
    expect(Ttt::Core::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
